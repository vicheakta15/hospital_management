name: hospital_management
description: A simple hospital management system in Dart
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # none needed yet

dev_dependencies:
  test: ^1.24.0
